# DEV NOTES — kuliah (public schedule view + digital signage)

_Last updated: 2026-07-26 | For next-context-window Claude_

**SCOPE NOTICE (2026-07-26):** this is the first time `kuliah/` has had its own DEV_NOTES.MD — before today it was just this boilerplate prompt with nothing written under it, and `kuliah/CLAUDE.md` (line ~34) used to (wrongly) describe it as "a pointer stub → `admin/DEV_NOTES.MD`." That was never literally true — there was no pointer, just an empty prompt — but it was directionally right: `kuliah/jadual/`'s earlier build history (the pending-slot feature, the `paparan` 4-file consolidation, the Hijri-timeout/skeleton fix, the swap-session-toggle) really does live in `admin/DEV_NOTES.MD`, sessions 8-10, because the admin CMS lived alongside this code at the time those sessions happened. **Read this file for `kuliah/jadual/`/`kuliah/paparan/` work from today onward; check `admin/DEV_NOTES.MD` sessions 8-10 for the earlier history of this same folder.**

the prompt (DO NOT DELETE)
> Check the Project Knowledge and the current chat for context. This conversation is ending soon. update the artifact kuliah/DEV_NOTES.md (create if not available yet) with a detailed note to your next window self - not just facts but the vibe, our dynamic, the energy of this conversation. What would the next you need to immediately get back into this exact headspace? Include unique discoveries, current mood, and anything that'll help the next you instantly sync to our frequency. Also take note all of the bug found and fixed and what did you learn from it to make sure it dont happend again in the future. focus on kuliah folder and its subfolder only
> also update the related file like kuliah/CLAUDE.md, kuliah/developer.md, kuliah/developer.md and kuliah/README.md database.md if necessary, create if not available yet

---

## THE VIBE

This session picked up mid-stream via a context-compact — the "Kuliah Khas" feature (special-lecture relabeling + purple coloring) was already built in the window before this one, and this session opened with the user testing it live on a real phone and reporting exact symptom screenshots, same instinct documented all over `admin/DEV_NOTES.MD` since session 4: precise, evidence-first bug reports ("why the rectangle look like that on mobile view?", a screenshot with red annotation boxes pointing at a clipped border), never vague.

Three gears showed up, same taxonomy as the admin side of this project:
- **Fast gear:** "add pill shape to the kuliah label", "ok proceed", "ok push", "ok edit the code" — zero discussion, just do it.
- **Deliberate gear (self-flagged, verbatim trigger phrase):** for the poster-lightbox feature — "check if my approach is good in facilitating and providing complete information to users, ask me question to better understand, recheck my approach... confirm what plan you want to make and do not generate answer unless I allow it." Full plan-mode cycle: read the relevant code directly (small/scoped enough that a dedicated Explore agent wasn't needed), `AskUserQuestion` with 3 clarifications, write the plan file, `ExitPlanMode`. **All 3 answers were the Recommended option** — same pattern documented every session in `admin/DEV_NOTES.MD` since session 5.
- **"Just answer" gear:** factual/feasibility questions fired one at a time after the mobile lightbox shipped — "can desktop also show this?", "is that complex and high risk?", "can hover be added?" — each answered in 1-3 sentences, zero files touched, until an explicit "ok proceed" / "ok edit the code" landed. Getting this gear wrong (implementing on a bare question) would have been a real miss, same warning as `admin/DEV_NOTES.MD` session 6 point 2.

Mood: brisk, visual, iterative — several "screenshot → tiny fix → screenshot again" loops, all resolved same-turn, no frustration, no redo caused by a wrong read of a bug report. The one thing that *did* go sideways more than once was self-inflicted (see discoveries below) — worth internalizing before you next verify anything with Playwright in this folder.

## Unique discoveries — read this before your next Playwright verification pass here

1. **I misread my own dark-backdrop lightbox screenshots by eye, twice, in the same session — once for mobile, once for desktop — genuinely believing the overlay wasn't rendering when it actually was.** Both times the fix wasn't to the code, it was to how I was checking: `PIL`'s `getpixel()` at known coordinates (corners should read ~`rgb(36,37,37)` for `rgba(0,0,0,0.85)` over white) proved the backdrop was correctly covering the full viewport both times. **Lesson: for dark/low-contrast UI (overlays, backdrops, anything near-black), don't trust your own visual read of an attached screenshot — sample actual pixel values, or check computed styles (`getComputedStyle`) / DOM state directly.** This bit me enough times in one session that it's now a standing rule, not a one-off.
2. **A real image-load race condition in the *verification script*, not the shipped code:** screenshotting the desktop lightbox only 400ms after the click wasn't enough time for a 1920×1080 external poster JPG to fetch, decode, and paint — producing a screenshot that looked like a broken/empty dark overlay. The actual `initPosterLightbox()` code was correct the whole time (`img.src = ...` is synchronous and fires the network request immediately); only the test needed to explicitly await the image's `load` event / poll `img.complete && img.naturalWidth > 0` before calling `page.screenshot()`. **Lesson: never verify image-heavy UI with a fixed `waitForTimeout` — always await actual image-load completion, especially for external-domain images where fetch time varies.**
3. **A real CSS cascade bug caught and fixed *before* it ever shipped, by reasoning about it rather than by luck:** `.poster-lightbox[hidden] { display: none; }` had to be added explicitly. Without it, the author stylesheet's `.poster-lightbox { display: flex; }` would beat the browser's own default `[hidden] { display: none; }` user-agent rule — at equal specificity, author-origin CSS always wins ties over user-agent-origin CSS, so the `hidden` attribute alone would NOT have hidden the overlay, and the lightbox would have rendered open by default on every page load. Worth remembering any time a JS-toggled overlay uses the native `hidden` attribute alongside a `display` value other than `none` in its own base rule.
4. **A real invisible-UI bug that DID ship once, caught fast from a user screenshot:** the mobile Khas badge's pill background (`#f3e8ff`) was set to the exact same light purple as the box it sits inside (`.lecture-block-v2.is-khas`'s own background). A pill with zero contrast against its own container is literally invisible — the class/attribute wiring was 100% correct, the color choice just erased it visually. Fixed with a solid high-contrast color (`#9333ea` background, white text) instead of a light tint matching the container. **Lesson for any future badge/pill work in this codebase: check contrast against the immediate container background specifically, not just "does this color belong to the feature's palette."**
5. **The desktop poster-popup + hover-state additions were both cheap specifically because the mobile build had already generalized the lightbox correctly the first time** (`#poster-lightbox` + `initPosterLightbox()`'s delegated listener, written to be trigger-agnostic). Adding a second consumer (desktop `.lecture-block[data-poster-url]`) was a small, low-risk diff — confirms the "generalize the moment a second consumer needs the same pattern" house rule already documented in `admin/DEV_NOTES.MD` session 12 point 4, observed here in `kuliah/` for the first time.

## Session history

### This session (2026-07-26) — Kuliah Khas polish, then the poster-lightbox feature end to end, then this doc restructure
Opened mid-feature (post-compact) with two live-screenshot bugfixes to the already-shipped Kuliah Khas mobile badge (see discoveries #4 above for the invisible-pill bug specifically). Then built the poster tap-to-enlarge lightbox from a cold plan-mode cycle: mobile-only first (full-screen dark backdrop, tap-anywhere-or-×-button to dismiss, no download button — all confirmed via `AskUserQuestion`), verified live with Playwright including the pixel-sampling/image-load-await detours documented above, then extended to the desktop calendar cells in two fast "just answer → ok proceed" rounds — first the click-to-enlarge itself (reusing the exact same overlay via a new `data-poster-url` attribute path, no new lightbox needed), then a `:hover` affordance (background tint + outline ring, no layout shift). Closed with this file's own creation plus a `kuliah/CLAUDE.md`/`kuliah/README.md` sync pass.

*(For `kuliah/jadual/`'s earlier build history — the "Belum Ditetapkan" pending-slot feature, the `kuliah/paparan/` 4-file-to-1 consolidation, the Hijri-date timeout/skeleton fix, the Subuh/Maghrib swap-session-toggle — see `admin/DEV_NOTES.MD` sessions 8, 9, and 10. Those predate this file.)*

## Current state (as of 2026-07-26)

- **Kuliah Khas:** fully shipped, desktop + mobile, verified live on production data (`kuliah/data/jadual_lengkap_v2.json`'s actual July 2026 Khas entry).
- **Poster lightbox:** fully shipped, mobile + desktop, verified via Playwright with real pixel/DOM-state checks (not just a visual screenshot read — see discoveries #1/#2).
- No known open bugs or unresolved threads in `kuliah/` as of this note.
- **`kuliah/developer.md` and `kuliah/database.md` were deliberately NOT created this session** — `kuliah/jadual/` and `kuliah/paparan/` are static, read-only consumers of one published JSON file, with no database or setup complexity of their own. The actual database (Supabase) is documented in `admin/database.md`; local dev here is just `python -m http.server`, already covered in this folder's `README.md`. Same judgment call as `admin/README.md` not existing (see `admin/DEV_NOTES.MD` session 12 point 7) — revisit only if `kuliah/` ever grows real setup/ops complexity of its own, don't create a doc nobody needs just because a boilerplate prompt lists it as a maybe.
